// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}


// data source and service usage
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
